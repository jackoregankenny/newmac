# newmac catalog — everything the picker offers

> Auto-generated by `scripts/docs.sh` from `scripts/catalog.sh` — do not edit by hand.
> ✓ = installed by default. Core tools install always.

## Core (always installed)

starship, zoxide, fzf, eza, bat, ripgrep, fd, delta, zsh-autosuggestions, zsh-syntax-highlighting, git, gh, jq, yq, wget, tree, btop, macmon

## Terminals

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Ghostty** (`ghostty`) | `brew install --cask ghostty` | Fast native GPU terminal — themed config included |
| ✓ | **Rio** (`rio`) | `brew install --cask rio` | Light GPU terminal, RetroArch shaders |
|  | **WezTerm** (`wezterm`) | `brew install --cask wezterm` | GPU terminal configured in Lua |
|  | **Alacritty** (`alacritty`) | `brew install --cask alacritty` | Minimal fast GPU terminal |
|  | **kitty** (`kitty`) | `brew install --cask kitty` | Feature-rich GPU terminal |
|  | **Warp** (`warp`) | `brew install --cask warp` | AI-native terminal (account required) |
|  | **iTerm2** (`iterm2`) | `brew install --cask iterm2` | The classic macOS terminal |

## Multiplexers

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Zellij** (`zellij`) | `brew install zellij` | Modern multiplexer — sessions/splits/layouts |
| ✓ | **tmux** (`tmux`) | `brew install tmux` | Classic multiplexer (SSH ubiquity) |

## Shells (zsh is the default)

| | Tool | Install | Description |
|---|---|---|---|
|  | **fish** (`fish`) | `brew install fish` | Great defaults, but not POSIX — the newmac rice is zsh-wired |
|  | **Nushell** (`nushell`) | `brew install nushell` | Structured-data shell — pipelines of tables, not text |

## AI coding agents

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Claude Code** (`claude`) | vendor script: `https://claude.ai/install.sh` | Anthropic agentic CLI |
| ✓ | **Codex** (`codex`) | `brew install --cask codex` | OpenAI Codex CLI |
| ✓ | **Factory Droid** (`droid`) | vendor script: `https://app.factory.ai/cli` | Factory agentic CLI |
| ✓ | **opencode** (`opencode`) | `brew install sst/tap/opencode` | Open-source agentic CLI (SST) |
|  | **Amp** (`amp`) | vendor script: `https://ampcode.com/install.sh` | Sourcegraph agentic CLI |
|  | **Kimi Code** (`kimi`) | vendor script: `https://code.kimi.com/kimi-code/install.sh` | Moonshot agentic CLI (K-series models) |
|  | **Crush** (`crush`) | `brew install charmbracelet/tap/crush` | Charm glamorous TUI coding agent |
|  | **Goose** (`goose`) | `brew install block-goose-cli` | Block/Linux Foundation open agent |
|  | **Cursor CLI** (`cursor`) | vendor script: `https://cursor.com/install` | Cursor agent in the terminal |
|  | **Copilot CLI** (`copilot`) | `bun i -g @github/copilot` | GitHub Copilot coding agent |
|  | **Aider** (`aider`) | `uv tool install aider-chat` | Open-source pair programmer |
|  | **Qwen Code** (`qwen`) | `bun i -g @qwen-code/qwen-code` | Alibaba Qwen coding CLI |
|  | **Gemini CLI** (`gemini`) | `brew install gemini-cli` | Google (deprecated in brew — successor: antigravity-cli) |

## Agent workbenches

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Orca** (`orca`) | `brew install --cask stablyai/orca/orca` | ADE — fleet of parallel agents in worktrees, browser + editor built in |
| ✓ | **cmux** (`cmux`) | `brew install --cask cmux` | Agent terminal for multi-agent work (libghostty) |
|  | **Conductor** (`conductor`) | `brew install --cask conductor` | Parallel Claude/Codex/Cursor agents in worktrees (Melty Labs) |
|  | **Nimbalyst** (`nimbalyst`) | `brew install --cask nimbalyst` | Visual workspace for Claude Code + Codex (was Crystal) |

## Local AI

| | Tool | Install | Description |
|---|---|---|---|
|  | **Ollama** (`ollama`) | `brew install ollama` | Run local LLMs from the terminal |
|  | **LM Studio** (`lm-studio`) | `brew install --cask lm-studio` | Local LLM GUI — download & chat with open models |

## Menu bar

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **SketchyBar** (`sketchybar`) | `brew install FelixKratz/formulae/sketchybar` | Scriptable status bar (configured by this repo) |
|  | **barik** (`barik`) | `brew install --cask mocki-toki/formulae/barik` | Modern SwiftUI menu bar — TOML config, AeroSpace-aware |
|  | **Ice** (`ice`) | `brew install --cask jordanbaird-ice` | Menu bar manager — hide/rearrange icons (Bartender alternative) |
| ✓ | **Stats** (`stats`) | `brew install --cask stats` | Menu-bar system monitor |

## Tiling & window management

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **AeroSpace** (`aerospace`) | `brew install --cask nikitabobko/tap/aerospace` | i3-like tiling WM — no SIP disable (the unixporn favourite) |
|  | **yabai** (`yabai`) | `brew install koekeishiya/formulae/yabai` | Most powerful tiling WM (full features need SIP disable; pair with skhd) |
|  | **skhd** (`skhd`) | `brew install koekeishiya/formulae/skhd` | Hotkey daemon for yabai (AeroSpace does not need it) |
|  | **Amethyst** (`amethyst`) | `brew install --cask amethyst` | Auto-tiling with a native feel, zero config (no SIP disable) |
|  | **Rectangle** (`rectangle`) | `brew install --cask rectangle` | Keyboard window snapping — light-touch, exec friendly |
|  | **Loop** (`loop`) | `brew install --cask loop` | Radial window snapping — pretty, exec friendly |
| ✓ | **JankyBorders** (`borders`) | `brew install FelixKratz/formulae/borders` | Focused-window border |
| ✓ | **AltTab** (`alt-tab`) | `brew install --cask alt-tab` | Windows-style window-level alt-tab |
|  | **DockDoor** (`dockdoor`) | `brew install --cask dockdoor` | Dock hover previews + another alt-tab flavour |
| ✓ | **Karabiner** (`karabiner`) | `brew install --cask karabiner-elements` | Keyboard remap (Caps → ⌘ held / Esc tapped) |
| ✓ | **Raycast** (`raycast`) | `brew install --cask raycast` | Launcher / window management |

## Dev runtimes & containers

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Bun** (`bun`) | `brew install oven-sh/bun/bun` | JS/TS runtime + package manager |
| ✓ | **fnm** (`fnm`) | `brew install fnm` | Fast Node version manager (installs Node LTS) |
| ✓ | **uv** (`uv`) | `brew install uv` | Python package/venv manager |
| ✓ | **Go** (`go`) | `brew install go` | Go toolchain |
| ✓ | **Rust** (`rust`) | rustup (sh.rustup.rs) | rustup + stable toolchain (the canonical way) |
| ✓ | **docker CLI** (`docker`) | `brew install docker` | Docker CLI (engine via Colima/OrbStack) |
| ✓ | **docker-compose** (`docker-compose`) | `brew install docker-compose` | Compose v2 |
| ✓ | **Colima** (`colima`) | `brew install colima` | Free container runtime — no Docker Desktop licence |
|  | **OrbStack** (`orbstack`) | `brew install --cask orbstack` | Fast Docker Desktop alternative (free for personal use) |

## Dev apps

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **VS Code** (`vscode`) | `brew install --cask visual-studio-code` | Editor |
|  | **Neovim** (`neovim`) | `brew install neovim` | Terminal editor — add NvChad/LazyVim yourself after |
|  | **Cursor** (`cursor-ide`) | `brew install --cask cursor` | AI editor (the app; CLI agent is separate) |
|  | **Zed** (`zed`) | `brew install --cask zed` | Fast collaborative editor |
|  | **GitHub Desktop** (`github-desktop`) | `brew install --cask github` | Git GUI |
|  | **TablePlus** (`tableplus`) | `brew install --cask tableplus` | Database GUI |
|  | **Bruno** (`bruno`) | `brew install --cask bruno` | Open-source API client |
|  | **Postman** (`postman`) | `brew install --cask postman` | API client |
|  | **UTM** (`utm`) | `brew install --cask utm` | Virtual machines on Apple Silicon |
|  | **Xcode** (`xcode`) | App Store (`mas`, id 497799835) | Full IDE + simulators (App Store, huge) |

## Browsers

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Dia** (`dia`) | `brew install --cask thebrowsercompany-dia` | The Browser Company |
|  | **Arc** (`arc`) | `brew install --cask arc` | The Browser Company (classic) |
|  | **Chrome** (`chrome`) | `brew install --cask google-chrome` | Google |
|  | **Firefox** (`firefox`) | `brew install --cask firefox` | Mozilla |
|  | **Brave** (`brave`) | `brew install --cask brave-browser` | Chromium + adblock |
|  | **Zen** (`zen`) | `brew install --cask zen` | Firefox-based, Arc-style UI |
|  | **Edge** (`edge`) | `brew install --cask microsoft-edge` | Microsoft (work-policy friendly) |
|  | **qutebrowser** (`qutebrowser`) | `brew install --cask qutebrowser` | Keyboard-driven, vim-style browser |

## Essentials & productivity

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **1Password** (`1password`) | `brew install --cask 1password` | Password manager (app) |
| ✓ | **1Password CLI** (`1password-cli`) | `brew install --cask 1password-cli` | `op` — terminal + SSH agent + shell plugins |
| ✓ | **coconutBattery** (`coconutbattery`) | `brew install --cask coconutbattery` | Battery health / cycle count |
|  | **Obsidian** (`obsidian`) | `brew install --cask obsidian` | Markdown notes |
|  | **Notion** (`notion`) | `brew install --cask notion` | Docs / wiki |
|  | **Figma** (`figma`) | `brew install --cask figma` | Design |
|  | **Linear** (`linear`) | `brew install --cask linear-linear` | Issue tracking |
|  | **Todoist** (`todoist`) | `brew install --cask todoist` | Tasks |
|  | **Keka** (`keka`) | `brew install --cask keka` | Archiver |
|  | **Shottr** (`shottr`) | `brew install --cask shottr` | Screenshot annotation |
|  | **Amphetamine** (`amphetamine`) | App Store (`mas`, id 937984704) | Keep the Mac awake (App Store) |

## Office & work

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Microsoft Teams** (`teams`) | `brew install --cask microsoft-teams` | Work chat & calls |
| ✓ | **Outlook** (`outlook`) | `brew install --cask microsoft-outlook` | Work email & calendar |
| ✓ | **Word** (`word`) | `brew install --cask microsoft-word` | Documents |
| ✓ | **Excel** (`excel`) | `brew install --cask microsoft-excel` | Spreadsheets |
| ✓ | **PowerPoint** (`powerpoint`) | `brew install --cask microsoft-powerpoint` | Slides |
|  | **OneDrive** (`onedrive`) | `brew install --cask onedrive` | Microsoft cloud storage |
|  | **LibreOffice** (`libreoffice`) | `brew install --cask libreoffice` | Free & open Office suite |
|  | **OnlyOffice** (`onlyoffice`) | `brew install --cask onlyoffice` | Best Office-format compatibility (open source) |

## Comms & media

| | Tool | Install | Description |
|---|---|---|---|
|  | **Slack** (`slack`) | `brew install --cask slack` | Team chat |
|  | **Discord** (`discord`) | `brew install --cask discord` | Chat |
|  | **Zoom** (`zoom`) | `brew install --cask zoom` | Video calls |
|  | **WhatsApp** (`whatsapp`) | `brew install --cask whatsapp` | Messaging |
|  | **Telegram** (`telegram`) | `brew install --cask telegram` | Messaging |
|  | **Spotify** (`spotify`) | `brew install --cask spotify` | Music |
|  | **spotify_player** (`spotify-player`) | `brew install spotify_player` | TUI Spotify client — rice-friendly (Premium required) |
|  | **kew** (`kew`) | `brew install kew` | TUI music player for local files |
|  | **IINA** (`iina`) | `brew install --cask iina` | Video player |
|  | **VLC** (`vlc`) | `brew install --cask vlc` | Plays anything |

## Network & VPN

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Cloudflare WARP** (`cloudflare-warp`) | `brew install --cask cloudflare-warp` | Encrypted DNS / VPN |
| ✓ | **Tailscale** (`tailscale`) | `brew install --cask tailscale-app` | Zero-config WireGuard mesh — your devices, one network |
|  | **Mullvad** (`mullvad`) | `brew install --cask mullvad-vpn` | Privacy VPN (no account details) |
|  | **Proton VPN** (`protonvpn`) | `brew install --cask protonvpn` | Privacy VPN |
|  | **WireGuard tools** (`wireguard`) | `brew install wireguard-tools` | wg / wg-quick CLI |

## Maintenance & cleanup

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **Mole** (`mole`) | `brew install mole` | Clean/uninstall/analyze/monitor from the terminal (run: mo) |
| ✓ | **dockutil** (`dockutil`) | `brew install dockutil` | Dock layout CLI — powers the Dock arranger |
| ✓ | **mas** (`mas`) | `brew install mas` | Mac App Store CLI — script App Store installs |
| ✓ | **dust** (`dust`) | `brew install dust` | Visual disk usage (better du) |
| ✓ | **duf** (`duf`) | `brew install duf` | Pretty disk free (better df) |
|  | **Topgrade** (`topgrade`) | `brew install topgrade` | Upgrade everything in one command (overlaps update.sh) |
|  | **Pearcleaner** (`pearcleaner`) | `brew install --cask pearcleaner` | Open-source app uninstaller (GUI) |
|  | **OnyX** (`onyx`) | `brew install --cask onyx` | Veteran macOS maintenance & tweaks GUI |

## Fonts

| | Tool | Install | Description |
|---|---|---|---|
| ✓ | **JetBrains Mono NF** (`font-jetbrains`) | `brew install --cask font-jetbrains-mono-nerd-font` | Default terminal font |
| ✓ | **Symbols NF** (`font-symbols`) | `brew install --cask font-symbols-only-nerd-font` | Icons for sketchybar etc. |
|  | **Fira Code NF** (`font-fira`) | `brew install --cask font-fira-code-nerd-font` | Ligature classic |
|  | **Meslo LG NF** (`font-meslo`) | `brew install --cask font-meslo-lg-nerd-font` | Powerlevel-style font |
|  | **Monaspace** (`font-monaspace`) | `brew install --cask font-monaspace` | GitHub superfamily |

## Presets

### Balanced (`--preset default`)

The author's daily setup — agents, tiling desktop, the works

_Uses the catalog defaults (✓ items above)._

### Minimal (`--preset minimal`)

Lean: Ghostty + Zellij + Claude + 1Password, no rice, no extras

Ghostty, Zellij, Claude Code, 1Password, 1Password CLI, VS Code, Bun, fnm, uv, Mole, mas, dust, duf, JetBrains Mono NF, Symbols NF

### Web dev (`--preset webdev`)

TypeScript/web: Bun, Node, containers, VS Code, Chrome, Figma

Ghostty, Rio, cmux, Orca, Zellij, tmux, Claude Code, Codex, opencode, Bun, fnm, uv, docker CLI, docker-compose, Colima, VS Code, Chrome, Figma, Dia, 1Password, 1Password CLI, Cloudflare WARP, Raycast, Stats, Mole, mas, dust, duf, JetBrains Mono NF, Symbols NF

### AI power user (`--preset ai`)

Every coding agent + agent terminals + full tiling desktop

Ghostty, Rio, cmux, Orca, Conductor, Nimbalyst, Zellij, tmux, Claude Code, Codex, Factory Droid, opencode, Amp, Kimi Code, Crush, Goose, Cursor CLI, Copilot CLI, Aider, Qwen Code, Bun, fnm, uv, Go, Rust, docker CLI, docker-compose, Colima, VS Code, Dia, 1Password, 1Password CLI, Cloudflare WARP, Raycast, Stats, coconutBattery, SketchyBar, AeroSpace, JankyBorders, AltTab, Karabiner, Mole, mas, dust, duf, JetBrains Mono NF, Symbols NF

### Full rice (`--preset rice`)

Linux-style desktop first: tiling, bar, borders, all the fonts

Ghostty, Rio, Zellij, tmux, Claude Code, Bun, fnm, uv, SketchyBar, Stats, AeroSpace, JankyBorders, AltTab, DockDoor, Karabiner, Raycast, 1Password, 1Password CLI, VS Code, Dia, Mole, mas, dust, duf, JetBrains Mono NF, Symbols NF, Fira Code NF, Meslo LG NF, Monaspace

## Themes

- **Catppuccin Mocha** (`catppuccin`)
- **Gruvbox Dark** (`gruvbox`)
- **Kanagawa Dragon** (`kanagawa`)
- **Nord** (`nord`)
- **Rosé Pine** (`rosepine`)
- **Tokyo Night** (`tokyonight`)

Switch any time: `bash scripts/theme.sh <id>`
